services:
  moneymissint-app:

    build: .

    ports:
      - "8080:8080"

    environment:
    - JWT_SECRET=ultramegasecretpasswordinHere2390481348139440582934324234567
    - SPRING_DATASOURCE_URL=jdbc:postgresql://host.docker.internal:5432/moneymissint

    - SPRING_DATASOURCE_USERNAME=emiliorevueltasv

    extra_hosts:
     - "host.docker.internal:host-gateway"

